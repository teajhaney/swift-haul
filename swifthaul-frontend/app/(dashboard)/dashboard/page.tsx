import { ComingSoon } from "@/components/shared/coming-soon";

/**
 * Dashboard analytics page — KPI cards, charts, and recent orders.
 * Content will be built in the next phase.
 */
export default function DashboardPage() {
  return (
    <ComingSoon
      feature="Analytics Dashboard"
      description="KPI cards, delivery performance charts, and real-time order tracking are coming next."
    />
  );
}
