import { ComingSoon } from "@/components/shared/coming-soon";

export default function SettingsPage() {
  return <ComingSoon feature="Settings" description="User management — invite, list, and deactivate team members — coming next." />;
}
